#!/bin/sh

# Defaults
lines=2
border="$DMENU_BORDER"
height="$DMENU_HEIGHT"
prompt="󰄠 iOS Mounter:"

# Command
choice=$(printf "󰄟 Mount\n󰮑 Eject\n" | dmenu -l $lines -bw $border -h $height -p "$prompt" -c )

case "$choice" in
  "󰄟 Mount") ios-mount ;;
  "󰮑 Eject") ios-unmount ;;
esac
