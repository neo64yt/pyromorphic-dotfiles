#!/bin/sh

# This script is used for executing a command

# Defaults
lines="$DMENU_LINES"
border="$DMENU_BORDER"
height="$DMENU_HEIGHT"
prompt="󰆍 Run:"

# Command
dmenu_run -c -n -l $lines -bw $border -h $height -p "$prompt" 



# vim: ft=sh
